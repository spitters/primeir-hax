# Validation manifest — see scripts/hax-validation/README.md
primitive = "Prime-field / EC arithmetic over GF(2^255-19) (Curve25519 base field + edwards25519, RFC 8032)"
reference = "RFC 8032 edwards25519 published constants: base point B, curve parameter d, and the prime subgroup order L = 2^252 + 27742317777372353535851937790883648493. Field law anchored by (p-1)^2 = 1 mod p; group law anchored by [L]B = O and [L+1]B = B."
reference_kind = "rfc-vectors"
test_files = ["tests/kat.rs"]
notes = "kat.rs asserts (i) field identities (a*ONE=a, a*inv(a)=ONE, distributivity, (p-1)^2=1) cross-checked against num-bigint directly; (ii) the Montgomery contract from_mont(to_mont a)=a and mont_mul(to_mont a, to_mont b)=to_mont(a*b); (iii) the EC group law against RFC 8032 published values — [L]B = identity and [L+1]B = B, where L is the published edwards25519 subgroup order and B the published base point. These are independent published facts, not self-referential KATs. The reference instance is the OPAQUE arithmetic leaf; hax extracts only the trait surface."
